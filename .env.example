# Backend Environment Configuration Template
# Duplicate this file as .env and populate with your credentials.
# NEVER commit your real production .env into source control!

PORT=5000
NODE_ENV=production
JWT_SECRET=your_generated_cryptographic_jwt_secret_here
SUPER_ADMIN_MOBILE=8278270339
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/studyMaterialApp?retryWrites=true&w=majority
